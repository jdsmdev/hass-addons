# Portfolio Builder add-on

Runs the Portfolio Builder web application and its optional earnings experiment through Home Assistant Ingress.

The application image is built from the Portfolio Builder source repository and persistent state is stored in the add-on's `/data` volume.
